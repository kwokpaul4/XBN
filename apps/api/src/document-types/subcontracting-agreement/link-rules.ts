import type { LinkRule } from '@xbn/document-core';

/** Anchor; targets only. Phase 3.3 SUBCONTRACT_* docs register their own
 *  reverse rules. */
export const subcontractingAgreementLinkRules: ReadonlyArray<LinkRule> = [];
