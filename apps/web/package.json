{
  "name": "@xbn/web",
  "version": "0.0.0",
  "private": true,
  "description": "XBN web portal — React + Vite single app with role-based routes (placeholder; scaffold in Phase 1.4)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
