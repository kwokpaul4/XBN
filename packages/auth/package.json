{
  "name": "@xbn/auth",
  "version": "0.0.0",
  "private": true,
  "description": "XBN identity & authentication — sessions, password hashing, multi-org membership.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.0",
    "@oslojs/crypto": "^1.0.1",
    "@oslojs/encoding": "^1.1.0",
    "@xbn/db": "workspace:*"
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
