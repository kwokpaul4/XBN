{
  "name": "@xbn/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema and client for XBN. Schema lands in Phase 1.3 (Org + TradingRelationship) and Phase 1.5 (documents substrate).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
