{
  "name": "@xbn/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Types shared between @xbn/api and @xbn/web (DTO shapes, enums, etc.).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.2"
  }
}
